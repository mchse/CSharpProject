@*Developed by M.Chasse*@

@model Project.Models.Customer
@{
    ViewBag.Title = "Customer Library";
    Layout = "~/Views/Shared/_Layout.cshtml";

    /*get cookie data*/
    var cookieId = Request.Cookies["UserID"];
    string custID = "";
    if (cookieId != null)
    {
        custID = cookieId.Value;
    }

    var cookieUsername = Request.Cookies["Username"];
    string Username = "";
    if (cookieUsername != null)
    {
        Username = cookieUsername.Value;
    }
}

<h2>@Username 's Streamsong Library</h2>
@*for debug, will take out: *@
<p>Your user id is: @Model.Id</p>

<h3>Saved Songs</h3>

<table class="table table-striped">
    <thead>
    <tr>
        <th>Title</th>
        <th>Artist</th>
    </tr>
    </thead>
    <tbody>
    
   @* @foreach (var song in Model)*@
    {
        <tr>
            @*<td><a href ="/Customers/Details/@customer.Id">@customer.Name</a></td>*@
            @* <td>@Html.ActionLink(song.Title, "CustomerLibrary", "Library", new {id = song.Id}, null)</td>*@

            @*<td>@Html.ActionLink("Register", "RegisterWorkshop", "Workshops", new { id = workshop.Id, userID = userID }, null)</td>*@
            @*<td><a href="/Workshop/RegisterWorkshop/@song.Id/@custID">Register</a></td>*@
        </tr>
    }
    </tbody>
</table>

@*@Html.ActionLink("FIND more songs to add", "SongLibrary", "Song");*@